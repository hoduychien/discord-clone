const APP_ENV = {
  mode: {
    development: 'development',
    production: 'production'
  },
  baseUrl: import.meta.env.BASE_URL,
  domainUrl: import.meta.env.DOMAIN
};

export default APP_ENV;
